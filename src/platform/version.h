#pragma once
// VERTEX_VERSION is defined by CMake from the latest git tag.
// See CMakeLists.txt: add_compile_definitions(VERTEX_VERSION="...")
#ifndef VERTEX_VERSION
#define VERTEX_VERSION "0.0.0"
#endif
