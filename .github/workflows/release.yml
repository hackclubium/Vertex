name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            name: windows
            configure: cmake -B build -A x64
          - os: macos-latest
            name: macos
            configure: cmake -B build
          - os: ubuntu-latest
            name: linux
            configure: cmake -B build

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Linux deps
        if: matrix.name == 'linux'
        run: sudo apt-get update && sudo apt-get install -y libgtk-3-dev

      - name: Configure
        run: ${{ matrix.configure }}

      - name: Build
        run: cmake --build build --config Release

      - name: Package macOS bundle
        if: matrix.name == 'macos'
        run: cd build && zip -r Vertex-macos.zip Vertex.app

      - name: Upload artifact (Windows)
        if: matrix.name == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: vertex-windows
          path: build/Release/Vertex.exe

      - name: Upload artifact (macOS)
        if: matrix.name == 'macos'
        uses: actions/upload-artifact@v4
        with:
          name: vertex-macos
          path: build/Vertex-macos.zip

      - name: Upload artifact (Linux)
        if: matrix.name == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: vertex-linux
          path: build/Vertex

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          tag="${GITHUB_REF_NAME}"
          mv vertex-windows/Vertex.exe Vertex-windows.exe || true
          mv vertex-macos/Vertex-macos.zip Vertex-macos.zip || true
          mv vertex-linux/Vertex Vertex-linux || true
          chmod +x Vertex-linux || true

          gh release view "$tag" --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1 && \
            gh release upload "$tag" Vertex-* --repo "$GITHUB_REPOSITORY" --clobber || \
            gh release create "$tag" Vertex-* --repo "$GITHUB_REPOSITORY" --title "$tag" --generate-notes
